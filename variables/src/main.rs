fn main() {
    // Declaring a Constant: const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;
    // Deckaring a immutable variable: let x = 5;
    // Declaring a mutable variable: let mut x = 5;
    // Printing a Variable: println!("The Value of x is: {}", x);
    // Changing a variable that is mutable: x = 6;

    // Shadowing Variables: 
    // let x = 5;

    // let x = x + 1;

    // {
    //     let x = x * 2;
    //     println!("The value of x in the inner scope is: {x}");
    // }

    // println!("The value of x is: {x}");

    // When using Shadowing, you can also change the type of the variable:
    // let spaces = "    "
    // let spaces = spaces.len();
}